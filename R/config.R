opt_setup <- "uncovr.setup"
plan_file_name <- "_plan.rds"
setup_file_name <- "_setup.rds"
last_tests_file_name <- "_last-tests.rds"
last_coverage_file_name <- "_last-coverage.rds"
report_dir_name <- "_coverage-report"
markdown_dir_name <- "_markdown-report"
cov_dir_name <- "_cov"
cov_sub_dir_name <- file.path(cov_dir_name, "sub")
